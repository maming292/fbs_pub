import { FixedNumberPipe } from './fixed-number.pipe';

describe('FixedNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new FixedNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
